import { ConvertToEurPipe } from './convert-to-eur.pipe';

describe('ConvertToEurPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToEurPipe();
    expect(pipe).toBeTruthy();
  });
});
